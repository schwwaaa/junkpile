{
  "name": "21-tauri-v2-projection-mapper",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri v2 two-window WebGL projection mapper with editable subdivision mesh, edge feathering, monitor placement, presets, native media loading, and snapshots",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.5"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.4.0"
  }
}
