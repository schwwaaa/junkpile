{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core events plus native media and snapshot dialogs for the Tauri v2 projection mapper",
  "windows": ["controls", "output"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
