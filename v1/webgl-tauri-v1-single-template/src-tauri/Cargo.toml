[package]
name    = "webgl-tauri-v1-single-template"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "1", features = [] }

[dependencies]
tauri = { version = "1", features = ["wry"] }

[features]
default          = []
custom-protocol  = ["tauri/custom-protocol"]
